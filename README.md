# bostrot/homebrew-tap

Homebrew casks for [WSL Manager](https://wslmanager.com/).

```bash
brew tap bostrot/tap
brew install --cask wsl-manager
```

Or in one line: `brew install --cask bostrot/tap/wsl-manager`.
